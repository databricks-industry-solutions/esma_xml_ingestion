bundle:
  name: esma_xml_ingestion

include:
  - resources/**/*.yml
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    variables:
      catalog: "esma_dev"
      
  prod:
    mode: production
    variables:
      catalog: "esma_prod"
