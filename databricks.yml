bundle:
  name: esma_xml_ingestion

include:
  - resources/**/*.yml
  - resources/*.yml
  - resources/config/local/*.yml

targets:
  dev:
    mode: development
    default: true
    variables:
      catalog: "esma_dev"
    resources:
      pipelines:
        emir_pipeline:
          development: true
        mifir_pipeline:
          development: true

  prod:
    mode: production
    variables:
      catalog: "esma_prod"
    resources:
      pipelines:
        emir_pipeline:
          development: false
        mifir_pipeline:
          development: false
